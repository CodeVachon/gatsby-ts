import React, { FC } from "react";
import { SEO } from "./../components";

const HomaePage: FC<{}> = ({}) => {
    return (
        <>
            <SEO pageTitle="Homepage" />
            <h1>HomePage</h1>
        </>
    );
};

export default HomaePage;
